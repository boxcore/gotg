# TODO


- [ ] 在默认情况下，许多剪辑软件或压制工具（包括未做特殊设置的 FFmpeg）在生成 MP4 时，会把 moov 写入到文件的末尾。导致的结果是：
     - 普通本地播放器：无所谓，因为本地读取文件尾部只需要几毫秒。
     - 网络流媒体（Telegram 预览）：客户端从文件的第 0 个字节开始下载。当它发现文件的头部没有 moov 索引时，它就不知道该怎么播放。为了找到 moov，Telegram 只能被迫把整个视频文件全部下载完，读到文件末尾的索引后，才能拉起内置播放器。
    - 解决方案是：不需要重新编码视频（不需要重压，不会损失画质），只需要用 ffmpeg 调整一下文件结构，把 moov 提到最前面。这个过程属于“流拷贝”，几秒钟就能完成。类型命令如：

    ```bash
    ffmpeg -i 苹果需要下载完成才能预览1.mp4 -c copy -movflags +faststart 优化后的视频.mp4
    ```


